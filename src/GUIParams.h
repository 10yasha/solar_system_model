#pragma once

// need to move put in some class
//// parameter for amount of days that will elapse per second of real time
//extern double daysPerSecond = 0.2f;
//// whether objects will move in their orbits
//extern bool enableOrbitalMotion = true;

extern double daysPerSecond;
extern bool enableOrbitalMotion;